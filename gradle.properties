# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Gradle Daemon - Parallel execution for faster builds
org.gradle.parallel=true

# Gradle Build Cache - Reuse outputs from previous builds
org.gradle.caching=true

# Configuration on Demand - Only configure relevant projects
org.gradle.configureondemand=true

# Kotlin Incremental Compilation
kotlin.incremental=true
kotlin.incremental.useClasspathSnapshot=true

# Kotlin Code Style
kotlin.code.style=official

# AndroidX Support
android.useAndroidX=true

# Non-Transitive R Classes - Reduces R class size
android.nonTransitiveRClass=true

# Non-Final Resource IDs - Allows resource ID inlining
android.nonFinalResIds=true